# Progress Log — 2026-06-15

> Timezone: EEST (UTC+3). Times are local. Each entry corresponds to a finalized,
> committed piece of work. Latest entry at the bottom.

**Summary of the day:** Bootstrapped the KmmRepo project from an empty folder to a working
authenticated app — repo + Docker infra, the registration/login auth slice, Swagger API
docs, the super-admin user-management console, a feature reference doc, and a persisted
dark/light theme toggle.

---

## 14:18 — Project scaffold + Authentication & Registration (`P0-F001`)
**Commit:** `4e37b1d` (slice 1)

- Initialized the repo: npm-workspaces monorepo, `.gitignore`, `.env.example`,
  `docker-compose.yml` (Postgres 15 + Redis 7), `README.md`, moved the guide to `docs/`.
- **Backend:** Prisma `User` model + `init_user` migration + seed super-admin;
  `POST /auth/register|login|refresh|logout`; argon2 hashing; JWT access + rotating refresh
  tokens with Redis-backed `jti` rotation/revocation; httpOnly refresh cookie; zod
  validation; uniform error envelope; light audit logging. **8 vitest/supertest tests.**
- **Frontend:** Vite + React 18 + MUI + React Query; axios client with in-memory access
  token + single-flight refresh interceptor; `AuthContext`; `ProtectedRoute`; Login,
  Register, and a Dashboard placeholder.
- **Decision:** registration creates a `PENDING` account; only `ACTIVE` users can log in.
- **Verified:** live curl of the full auth flow + login gating; tests + typecheck green.
- Pushed to GitHub (`github.com/khaledmohsenmohammed/KmmRepo`).

## 14:38 — API documentation with Swagger / OpenAPI
**Commit:** `b25e45c`

- Added an OpenAPI 3 spec (`backend/src/docs/openapi.ts`, typed object) and served
  **Swagger UI at `/api/v1/docs`** + raw spec at `/api/v1/openapi.json` via
  `swagger-ui-express`.
- Documented it in the guide (§8.1) and added OpenAPI upkeep to the Definition of Done.
- **Verified:** `/docs` returns 200, all paths listed.

## 14:53 — Lint-format auth routes (PR workflow practice)
**Commits:** `d7e0085` → merge `7418eef`

- Reformatted `backend/src/routes/auth.ts` (no behavior change) on a feature branch and
  merged into `main`. Established the branch → merge flow for the solo repo.

## 15:15 — Super-admin user management & approval console (`P0-F002`, user side)
**Commits:** `d929ec7` → merge `1456654`

- **Backend:** `User` soft-delete fields + `user_soft_delete` migration; `requireSuperAdmin`
  guard; `/api/v1/admin/users` routes (auth + role gated) for list/filter, approve, reject,
  activate, deactivate, soft-delete, restore. **SUPER_ADMIN targets are protected (403)**;
  disabling/deleting revokes the user's refresh tokens; every action is audited. OpenAPI
  extended. **10 new tests (18 total).**
- **Frontend:** `api/admin.ts`; `AdminRoute` (SUPER_ADMIN-only) guard; shared `AppLayout`
  shell with conditional **Admin** nav link; Admin page with Pending/Active/Disabled/Deleted
  tabs, per-status actions, confirm dialogs, snackbars, and protected rows.
- **Decisions:** Approve → `ACTIVE` + `USER`; Reject and Not-active both → `DISABLED`;
  Delete = soft delete (recoverable). Per-project roles/access deferred until Projects exist.
- **Verified:** end-to-end curl (approve/reject/protection/authz/validation/soft-delete +
  restore), 18 tests, typecheck, frontend build — all green.

## 15:18 — Feature reference doc
**Commit:** `64ea8f9`

- Added `docs/features/registration-and-approval.md`: the full registration→approval story
  (user stories, flow, rules, data model, API reference, token design, code map, edge cases,
  verification). Started the `docs/features/` convention.

## 15:32 — Persisted dark/light theme toggle (part of `P0-F009`)
**Commit:** `78c6515`

- Made the MUI theme mode-aware (`buildTheme(mode)`); `ColorModeProvider` persists the mode
  to `localStorage` (falls back to OS preference); added a 🌙/☀️ toggle to the navbar.
- **Verified:** frontend build green; app served and opened.

---

### End-of-day state
- **Branch:** `main` @ `78c6515`, working tree clean, all work pushed.
- **Tests:** backend 18/18 passing. **Builds:** backend typecheck + frontend build green.
- **Phase 1:** `P0-F001` done; `P0-F002` user-management done (project-access deferred);
  dark/light toggle (`P0-F009`) started.
- **Next up:** `P0-F003` — Projects + nested folders + tree view (also unlocks the deferred
  project-access part of the admin console).
